# 使用するフォントファミリーを指定します。
font-family = "Moralerspace Argon"

# 要求されたフォントスタイルに使用する名前付きフォントスタイルを指定します。
font-style = "default"
# 太字スタイルに使用する名前付きフォントスタイルを指定します。
font-style-bold = "default"
# 斜体スタイルに使用する名前付きフォントスタイルを指定します。
font-style-italic = "default"
# 太字斜体スタイルに使用する名前付きフォントスタイルを指定します。
font-style-bold-italic = "default"

# ネイティブスタイルがない場合にフォントスタイルを合成するかどうかを制御します。
font-synthetic-style = "bold,italic,bold-italic"

# フォントフィーチャーを適用します（例：リガチャの無効化 `-calt`）。
font-feature = "calt, liga, dlig, ordn"

# フォントサイズをポイント単位で指定します。
font-size = 13

# 可変フォントのバリエーション値を設定します。
# font-variation =
# 太字スタイル用の可変フォントのバリエーション値を設定します。
# font-variation-bold =
# 斜体スタイル用の可変フォントのバリエーション値を設定します。
# font-variation-italic =
# 太字斜体スタイル用の可変フォントのバリエーション値を設定します。
# font-variation-bold-italic =

# 特定のUnicodeコードポイントを指定したフォントに強制的にマッピングします。
# font-codepoint-map =

# フォントを太い線で描画します（macOSのみ）。
font-thicken = false
# `font-thicken`が有効な場合の太さの強度を設定します（0-255）。
font-thicken-strength = 255

# テキストの字形処理を分割する位置を指定します（例：カーソル位置）。
font-shaping-break = "cursor"

# アルファブレンディング処理に使用するカラースペースを指定します。
alpha-blending = "native"

# セルの幅を整数またはパーセントで調整します。
adjust-cell-width = "0%"
# セルの高さを整数またはパーセントで調整します。
adjust-cell-height = "0%"
# テキストのベースライン位置をピクセルまたはパーセントで調整します。
adjust-font-baseline = "0%"
# アンダーラインの位置をピクセルまたはパーセントで調整します。
adjust-underline-position = "0%"
# アンダーラインの太さをピクセルまたはパーセントで調整します。
adjust-underline-thickness = "0%"
# ストライクスルーの位置をピクセルまたはパーセントで調整します。
adjust-strikethrough-position = "0%"
# ストライクスルーの太さをピクセルまたはパーセントで調整します。
adjust-strikethrough-thickness = "0%"
# オーバーラインの位置をピクセルまたはパーセントで調整します。
adjust-overline-position = "0%"
# オーバーラインの太さをピクセルまたはパーセントで調整します。
adjust-overline-thickness = "0%"
# バーカーソルとアウトライン矩形カーソルの太さを調整します。
adjust-cursor-thickness = "0%"
# すべてのカーソルタイプの高さを調整します。
adjust-cursor-height = "0%"
# ボックス描画文字の太さを調整します。
adjust-box-thickness = "0%"
# nerd fontアイコンの最大高さを調整します。
adjust-icon-height = "-10%"

# グラフエムクラスターのセル幅計算方法を指定します（`unicode`または`legacy`）。
grapheme-width-method = "unicode"

# FreeTypeのロード時に有効化するフラグを指定します（Linuxのみ）。
freetype-load-flags = "hinting,no-force-autohint,no-monochrome,autohint"

# 使用するテーマを指定します（組み込み名、カスタム名、またはファイルパス）。
theme = "Catppuccin Macchiato"

# ウィンドウの背景色を16進数またはX11カラー名で指定します。
# background = "#282c34"
# ウィンドウの前景色を16進数またはX11カラー名で指定します。
# foreground = "#ffffff"

# ターミナルの背景画像をPNGまたはJPEGファイルパスで指定します。
background-image = "~/Pictures/Pictures.library/images/MFSBCAFBH2356.info/lh3.png"
# 背景画像の不透明度を`background-opacity`に対する相対値で設定します。
background-image-opacity = 0.2
# 背景画像の表示位置を指定します（`center`, `top-left`など）。
background-image-position = "center"
# 背景画像の表示方法を指定します（`contain`, `cover`など）。
background-image-fit = "cover"
# ターミナル領域を埋めるために背景画像を繰り返すかどうかを指定します。
background-image-repeat = false

# 選択範囲の前景色を指定します。
# selection-foreground =
# 選択範囲の背景色を指定します。
# selection-background =

# 入力時に選択範囲のテキストをクリアするかどうかを指定します。
selection-clear-on-typing = true
# コピー操作後に選択範囲をクリアするかどうかを指定します。
selection-clear-on-copy = false

# 前景色と背景色の間に必要な最小コントラスト比を指定します。
minimum-contrast = 2

# 256色カラーパレットを`N=COLOR`形式で設定します。
# palette = "0=#1d1f21"

# カーソルの色を指定します（`cell-foreground`, `cell-background`も可）。
cursor-color = "cell-foreground"
# カーソルの不透明度レベルを指定します（0.0-1.0）。
cursor-opacity = 1.0
# デフォルトのカーソルスタイルを指定します（`block`, `bar`, `underline`など）。
cursor-style = "block"
# カーソルの点滅状態のデフォルト設定を行います。
cursor-style-blink = true
# カーソル位置のテキスト色を指定します（`cell-foreground`, `cell-background`も可）。
# cursor-text =

# alt/option+クリックでプロンプト上のカーソルを移動できる機能を有効にします。
cursor-click-to-move = true

# タイピング中にマウスカーソルを非表示にするかどうかを指定します。
mouse-hide-while-typing = false
# マウスクリック時にShiftキーが押されたことをプログラムが検出するかどうかを指定します。
mouse-shift-capture = false
# マウスホイールによるスクロール量を調整する倍率を指定します。
mouse-scroll-multiplier = 2.0

# どのタイミングでサーフェスを最下部までスクロールするかを指定します。
scroll-to-bottom = "keystroke,no-output"

# 背景の不透明度レベルを指定します（0.0-1.0）。
background-opacity = 0.9
# 明示的に背景色が設定されたセルにも背景不透明度を適用するかどうかを指定します。
background-opacity-cells = true
# 背景の不透明度が1未満の場合に背景をぼかすかどうか、またその強度を指定します。
background-blur = true

# フォーカスされていない分割領域の不透明度レベルを指定します。
unfocused-split-opacity = 0.7
# フォーカスが外れている分割領域を暗くする色を指定します。
# unfocused-split-fill =
# 分割線の色を指定します。
# split-divider-color =

# 新しいターミナルで実行するコマンド（通常はシェル）を指定します。
command = ""
# Ghostty起動時の最初のターミナルでのみ実行するコマンドを指定します。
initial-command = ""
# ターミナル内で起動されるコマンドに渡す追加の環境変数を設定します。
# env =
# 起動時にコマンドに標準入力として送信するデータを指定します。
# input =
# コマンド終了後もキー入力があるまでターミナルを開いたままにするかどうかを指定します。
wait-after-command = false
# プロセスの異常終了と見なす実行時間（ミリ秒）の下限値を設定します。
abnormal-command-exit-runtime = 250

# スクロールバックバッファのサイズをバイト単位で指定します。
scrollback-limit = 10000000

# URLのマッチングとデフォルトブラウザでのオープン機能を有効にするかどうかを指定します。
link-url = true
# マッチしたURLのリンクプレビューを表示するかどうかを指定します。
link-previews = true

# 新しいウィンドウを最大化状態で開始するかどうかを指定します。
maximize = false
# 新しいウィンドウをフルスクリーンで開始するかどうかを指定します。
fullscreen = false

# ウィンドウのタイトルを強制的に固定します。
# title =
# アプリケーションのクラス値を設定します（X11/Wayland）。
# class =
# WM_CLASSのインスタンス名フィールドを設定します（X11のみ）。
# x11-instance-name =

# コマンド開始後に変更するディレクトリを指定します。
# working-directory =

# キーの組み合わせとアクションを`trigger=action`形式で設定します。
keybind = "global:alt+`=toggle_quick_terminal"
keybind = "super+shift+d=new_split:down"
keybind = "super+enter=toggle_fullscreen"
keybind = "super+alt+shift+j=write_screen_file:open"
keybind = "super+2=goto_tab:2"
keybind = "super+digit_6=goto_tab:6"
keybind = "super+k=clear_screen"
keybind = "super+page_down=scroll_page_down"
keybind = "super+4=goto_tab:4"
keybind = "super+shift+z=redo"
keybind = "super+shift+v=paste_from_selection"
keybind = "super+w=close_surface"
keybind = "shift+page_down=adjust_selection:page_down"
keybind = "super+7=goto_tab:7"
keybind = "shift+arrow_down=adjust_selection:down"
keybind = "super+bracket_left=goto_split:previous"
keybind = "super+5=goto_tab:5"
keybind = "super+3=goto_tab:3"
keybind = "super+a=select_all"
keybind = "super+arrow_left=text:\x01"
keybind = "shift+page_up=adjust_selection:page_up"
keybind = "super+0=reset_font_size"
keybind = "super+digit_2=goto_tab:2"
keybind = "super+shift+bracket_left=previous_tab"
keybind = "super+shift+,=reload_config"
keybind = "super+,=open_config"
keybind = "super+ctrl+shift+j=write_screen_file:copy"
keybind = "paste=paste_from_clipboard"
keybind = "super+bracket_right=goto_split:next"
keybind = "super+alt+arrow_up=goto_split:up"
keybind = "super+ctrl+arrow_right=resize_split:right,10"
keybind = "alt+arrow_left=esc:b"
keybind = "super+equal=increase_font_size:1"
keybind = "ctrl+tab=next_tab"
keybind = "super+digit_4=goto_tab:4"
keybind = "super+q=quit"
keybind = "super+shift+arrow_up=jump_to_prompt:-1"
keybind = "super+alt+arrow_right=goto_split:right"
keybind = "super+ctrl+equal=equalize_splits"
keybind = "super+digit_7=goto_tab:7"
keybind = "super+ctrl+f=toggle_fullscreen"
keybind = "super+backspace=text:\x15"
keybind = "super+6=goto_tab:6"
keybind = "super+page_up=scroll_page_up"
keybind = "super+1=goto_tab:1"
keybind = "super+alt+w=close_tab:this"
keybind = "super+end=scroll_to_bottom"
keybind = "super+-=decrease_font_size:1"
keybind = "super+arrow_up=jump_to_prompt:-1"
keybind = "super+shift+t=undo"
keybind = "super+alt+arrow_left=goto_split:left"
keybind = "super+z=undo"
keybind = "super+digit_8=goto_tab:8"
keybind = "super+9=last_tab"
keybind = "shift+arrow_up=adjust_selection:up"
keybind = "super+home=scroll_to_top"
keybind = "super+digit_1=goto_tab:1"
keybind = "super+shift+w=close_window"
keybind = "super+alt+shift+w=close_all_windows"
keybind = "super+ctrl+arrow_down=resize_split:down,10"
keybind = "super+t=new_tab"
keybind = "shift+end=adjust_selection:end"
keybind = "super+v=paste_from_clipboard"
keybind = "shift+arrow_right=adjust_selection:right"
keybind = "super+c=copy_to_clipboard"
keybind = "super+ctrl+arrow_left=resize_split:left,10"
keybind = "super+shift+enter=toggle_split_zoom"
keybind = "super+arrow_right=text:\x05"
keybind = "super++=increase_font_size:1"
keybind = "super+shift+p=toggle_command_palette"
keybind = "super+8=goto_tab:8"
keybind = "shift+arrow_left=adjust_selection:left"
keybind = "super+shift+arrow_down=jump_to_prompt:1"
keybind = "super+alt+arrow_down=goto_split:down"
keybind = "copy=copy_to_clipboard"
keybind = "super+shift+j=write_screen_file:paste"
keybind = "super+shift+bracket_right=next_tab"
keybind = "super+arrow_down=jump_to_prompt:1"
keybind = "ctrl+shift+tab=previous_tab"
keybind = "alt+arrow_right=esc:f"
keybind = "super+n=new_window"
keybind = "shift+home=adjust_selection:home"
keybind = "super+alt+i=inspector:toggle"
keybind = "super+digit_5=goto_tab:5"
keybind = "super+digit_3=goto_tab:3"
keybind = "super+ctrl+arrow_up=resize_split:up,10"
keybind = "super+d=new_split:right"

# ウィンドウの左右のパディングをポイント単位で指定します。
window-padding-x = 0
# ウィンドウの上下のパディングをポイント単位で指定します。
window-padding-y = 0
# セルサイズで割り切れない余分なパディングを均等に配置するかどうかを指定します。
window-padding-balance = true
# パディング領域の色を指定します（`background`または`extend`）。
window-padding-color = "background"

# レンダリングを画面のリフレッシュレートに同期させるかどうかを指定します（macOSのみ）。
window-vsync = true
# 新しいウィンドウ/タブが前の作業ディレクトリを継承するかどうかを指定します。
window-inherit-working-directory = true
# 新しいウィンドウ/タブが前のフォントサイズを継承するかどうかを指定します。
window-inherit-font-size = true
# ウィンドウの装飾（タイトルバー、境界線など）の表示設定を指定します。
window-decoration = "auto"
# ウィンドウとタブのタイトルに使用するフォントを指定します（GTKのみ）。
# window-title-font-family =
# ウィンドウのサブタイトルに表示するテキストを指定します（GTKのみ）。
window-subtitle = false
# ウィンドウのテーマを指定します（`auto`, `system`, `light`, `dark`）。
window-theme = "auto"
# ターミナルの色を解釈する際に使用するカラースペースを指定します（macOSのみ）。
window-colorspace = "srgb"
# 起動時のウィンドウの幅をグリッドセル単位で指定します。
window-width = 130
# 起動時のウィンドウの高さをグリッドセル単位で指定します。
window-height = 45
# 起動時のウィンドウのX座標（左からの位置）をピクセル単位で指定します。
# window-position-x =
# 起動時のウィンドウのY座標（上からの位置）をピクセル単位で指定します。
# window-position-y =
# ウィンドウの状態（位置、サイズ、タブなど）を保存・復元するかどうかを指定します（macOSのみ）。
window-save-state = "default"
# ウィンドウのリサイズをセルサイズの単位で行うかどうかを指定します（macOSのみ）。
window-step-resize = true
# 新しいタブが作成される位置を指定します（`current`または`end`）。
window-new-tab-position = "current"
# タブバーをいつ表示するかを指定します（`always`, `auto`, `never`）。
window-show-tab-bar = "auto"
# ウィンドウタイトルバーの背景色を指定します（GTKのみ）。
# window-titlebar-background =
# ウィンドウタイトルバーの前景色を指定します（GTKのみ）。
# window-titlebar-foreground =

# リサイズ時にターミナルのサイズを表示するオーバーレイをいつ表示するかを指定します。
resize-overlay = "after-first"
# リサイズオーバーレイの表示位置を指定します。
resize-overlay-position = "center"
# リサイズオーバーレイが表示される時間を指定します。
resize-overlay-duration = "750ms"

# マウスカーソル下の分割ペインにフォーカスを自動で移動するかどうかを指定します。
focus-follows-mouse = false

# プログラムによるクリップボードの読み取り許可を設定します（`ask`, `allow`, `deny`）。
clipboard-read = "ask"
# プログラムによるクリップボードへの書き込み許可を設定します（`allow`, `deny`）。
clipboard-write = "allow"
# クリップボードにコピーする際に末尾の空白を削除するかどうかを指定します。
clipboard-trim-trailing-spaces = true
# 安全でない可能性のあるテキストを貼り付ける前に確認するかどうかを指定します。
clipboard-paste-protection = true
# ブラケットペーストモード中の貼り付けを安全と見なすかどうかを指定します。
clipboard-paste-bracketed-safe = true

# プログラムによるターミナルタイトルの問い合わせを許可するかどうかを指定します。
title-report = false

# Kitty画像プロトコルなどで使用される画像データの最大メモリ使用量をバイト単位で指定します。
image-storage-limit = 320000000

# テキスト選択時に自動的にクリップボードにコピーするかどうかを指定します。
copy-on-select = true
# ターミナル上での右クリックの動作を指定します。
right-click-action = "context-menu"
# ダブルクリックなどを認識するためのクリック間隔をミリ秒単位で指定します。
click-repeat-interval = 0

# 追加で読み込む設定ファイルを指定します。
# config-file =
# デフォルトの設定ファイルパスを読み込むかどうかを指定します（CLI専用）。
config-default-files = true

# サーフェス（タブ/ペイン）を閉じる前に確認するかどうかを指定します。
confirm-close-surface = true
# 最後のウィンドウが閉じた後にアプリケーションを終了するかどうかを指定します。
quit-after-last-window-closed = false
# 最後のウィンドウが閉じてから終了するまでの遅延時間を設定します（Linuxのみ）。
# quit-after-last-window-closed-delay =
# 起動時に最初のウィンドウを作成するかどうかを指定します。
initial-window = true
# undo操作が利用可能な時間を指定します（macOSのみ）。
undo-timeout = "10s"

# クイックターミナルウィンドウの表示位置を指定します。
quick-terminal-position = bottom
# クイックターミナルのサイズをパーセントまたはピクセルで指定します。
quick-terminal-size = 25%, 100px
# クイックターミナルウィンドウのレイヤーを指定します（GTK Waylandのみ）。
gtk-quick-terminal-layer = "top"
# クイックターミナルウィンドウの名前空間を指定します（GTK Waylandのみ）。
gtk-quick-terminal-namespace = "ghostty-quick-terminal"
# クイックターミナルが表示される画面を指定します（macOSのみ）。
quick-terminal-screen = mouse
# クイックターミナルの表示/非表示アニメーションの時間を秒単位で指定します（macOSのみ）。
quick-terminal-animation-duration = 0.2
# フォーカスが外れたときにクイックターミナルを自動的に隠すかどうかを指定します。
quick-terminal-autohide = true
# macOSのSpacesを切り替えた際のクイックターミナルの動作を指定します（macOSのみ）。
quick-terminal-space-behavior = "move"
# クイックターミナルがキーボード入力を受け取る状況を指定します（Waylandのみ）。
quick-terminal-keyboard-interactivity = "on-demand"

# シェル統合の自動インジェクションを有効にするかどうか、またその方法を指定します。
shell-integration = "detect"
# 有効にするシェル統合の機能をカンマ区切りで指定します。
shell-integration-features = "cursor,no-sudo,title,no-ssh-env,no-ssh-terminfo"

# OSCシーケンスによる色情報問い合わせの応答フォーマットを指定します。
osc-color-report-format = "16-bit"

# キーボード入力を無効にするKAMモード（ANSIモード2）を許可するかどうかを指定します。
vt-kam-allowed = false

# デフォルトシェーダーの後に実行するカスタムシェーダーのファイルパスを指定します。
# custom-shader =
# カスタムシェーダー使用時にアニメーションループを実行するかどうかを指定します。
custom-shader-animation = true

# ターミナルベルの通知機能をカンマ区切りで指定します。
bell-features = "no-system,no-audio,attention,title,no-border"
# `audio`ベル機能が有効な場合に使用する音声ファイルのパスを指定します（GTKのみ）。
# bell-audio-path =
# `audio`ベル機能の音量を指定します（0.0-1.0、GTKのみ）。
bell-audio-volume = 0.5

# アプリ内通知（トースト）の表示/非表示を制御します（GTKのみ）。
app-notifications = "clipboard-copy,config-reload"

# macOSでネイティブではない高速なフルスクリーンモードを使用するかどうかを指定します。
macos-non-native-fullscreen = false
# macOSタイトルバーのウィンドウボタン（信号機ボタン）を表示するかどうかを指定します。
macos-window-buttons = hidden
# macOSのタイトルバーのスタイルを指定します（`native`, `transparent`, `tabs`, `hidden`）。
macos-titlebar-style = native
# macOSタイトルバーに作業ディレクトリのプロキシアイコンを表示するかどうかを指定します。
macos-titlebar-proxy-icon = "visible"
# DockのGhosttyアイコンにファイルをドロップした際の動作を指定します。
macos-dock-drop-behavior = "new-tab"
# OptionキーをAltキーとして扱うかどうか、また左右どちらのキーに適用するかを指定します。
macos-option-as-alt = right
# macOSのウィンドウシャドウを有効にするかどうかを指定します。
macos-window-shadow = true
# Dockとアプリケーションスイッチャーでアプリアイコンを隠すかどうかを指定します。
macos-hidden = "never"
# パスワードプロンプトを検出した際に自動でセキュア入力を有効にするかどうかを指定します。
macos-auto-secure-input = true
# セキュア入力が有効なときにグラフィカルな表示を行うかどうかを指定します。
macos-secure-input-indication = true
# macOSのショートカットアプリによるGhosttyの制御を許可するかどうかを指定します。
macos-shortcuts = "ask"
# macOSのアイコンをカスタマイズします。
macos-icon = "retro"
macos-icon-frame = "plastic"


# 各ターミナルを個別のLinux cgroupに入れるかどうかを指定します。
linux-cgroup = "never"
# 個々のターミナルプロセスのメモリ上限をバイト単位で設定します。
# linux-cgroup-memory-limit =
# 個々のターミナルプロセスのプロセス数上限を設定します。
# linux-cgroup-processes-limit =
# cgroupの初期化に失敗した場合にGhosttyの起動を中止するかどうかを指定します。
linux-cgroup-hard-fail = false

# GTKのOpenGLデバッグログを有効にするかどうかを指定します。
gtk-opengl-debug = false
# GTKアプリケーションをシングルインスタンスモードで実行するかどうかを指定します。
gtk-single-instance = "detect"
# GTKのタイトルバーをWMのシンプルなものではなくフル表示するかどうかを指定します。
gtk-titlebar = true
# GTKのタブバーを画面のどの辺に表示するかを指定します（`top`, `bottom`, `hidden`）。
gtk-tabs-location = "top"
# ウィンドウ最大化時にタイトルバーを隠すかどうかを指定します（GTKのみ）。
gtk-titlebar-hide-when-maximized = false
# GTKのツールバー（タブバー）の外観を指定します。
gtk-toolbar-style = "raised"
# GTKのタイトルバーのスタイルを指定します（`native`または`tabs`）。
gtk-titlebar-style = "native"
# GTKのタブをスペースに合わせて広げる（Gnomeスタイル）かどうかを指定します。
gtk-wide-tabs = true
# 追加で読み込むカスタムCSSファイルを指定します。
# gtk-custom-css =

# OSCシーケンスによるデスクトップ通知を許可するかどうかを指定します。
desktop-notifications = true

# 太字テキストに使用する色、または`bright`（明るいパレット色）を指定します。
# bold-color =
# 淡色表示テキストの不透明度を指定します（0.0-1.0）。
faint-opacity = 0.5

# `TERM`環境変数に設定する値を指定します。
term = "xterm-ghostty"
# `ENQ` (0x05) を受信した際に送信する文字列を指定します。
# enquiry-response =

# 非同期I/Oに使用する低レベルAPIを指定します（Linuxのみ）。
async-backend = "auto"

# 自動更新の動作を制御します（macOSのみ）。
auto-update = "download"
# 自動更新に使用するリリースチャネルを指定します（`stable`または`tip`）。
auto-update-channel = "stable"
